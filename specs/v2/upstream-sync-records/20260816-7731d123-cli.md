# Upstream Sync Record: `7731d123` CLI Diagnostics

Date: 2026-08-16

Outcome: approved for formal-branch fast-forward

## Fixed References

- Formal custom branch before finalization: `native-responses-v2`
- Old formal custom head: `40162a8c096e50848c72d17dce5a2922ecb66985`
- Existing tested custom head: `917635b5c02409c09b38977a8f2a203201e7dfce`
- Previous official V2 head: `825193400773cceab9b92f6bc63247d6dde3d580`
- New official V2 head: `7731d1235dec02a2f0fc8977e7140bf0d446fa9f`
- Integration branch: `integrate/v2-7731d123-cli`
- Tested integration code head: `3df41c2f430437867ee6b15371316d7ba7ecc7fb`
- Recovery branch: `integrate/v2-82519340-selected`
- Recovery head: `917635b5c02409c09b38977a8f2a203201e7dfce`

The finalization action is a strict fast-forward of `native-responses-v2` to
the commit containing this record. The recovery branch remains unchanged.

## Upstream Range Classification

The official range `825193400..7731d1235` contains seven CLI-only commits and
changes eleven upstream files.

| Official commit | Local action    | Local commit | Result                                                              |
| --------------- | --------------- | ------------ | ------------------------------------------------------------------- |
| `a48d44955`     | port            | `69e74cfde`  | Compiled Bun runtime uses `--no-warnings`.                          |
| `613c570a3`     | port            | `7e88ddfd4`  | Non-Windows heap snapshot signal added.                             |
| `01b7b53ee`     | port            | `88ef4fdff`  | Generated wrapper formatting preserved.                             |
| `0d7fe6e07`     | skip-equivalent | `532731f6d`  | Undefined Bun compiler executable path was already omitted locally. |
| `b080f216a`     | port            | `6a265daa5`  | Native CPU profiling and service propagation added.                 |
| `174c0a742`     | port            | `f94a4ef70`  | Generated CPU-profile formatting preserved.                         |
| `7731d1235`     | port            | `bd85fd68b`  | Debug config help output clarified.                                 |

`0d7fe6e07` and `532731f6d` use different source expressions but are
behaviorally equivalent. `compileExecutable()` returns a string path or
`undefined`; both implementations omit the compiler option only for the
undefined case. The local implementation keeps the explicit undefined check.

## Local Adaptations

- `d70a656de fix(cli): inherit CPU profile for standalone servers`
  connected the upstream profile feature to the custom default private
  `serve --stdio` lifecycle.
- `3df41c2f4 fix(cli): isolate CPU profile propagation`
  closed the review finding that an unmarked ambient profile path could leak
  through non-serve commands into managed service creation. A profile path is
  now propagated only with an internal explicit-source marker. Explicit flags
  win, marked private serve inheritance works, and non-serve commands plus
  managed service discovery ignore an unmarked ambient value.
- `4a2a0c0a6 docs(v2): preserve standalone CPU profiling`
  introduced stable customization ID `CUST-OPS-004` and corrected the
  `CUST-OPS-003` introduced commit.

## Path Closure

The final custom delta from `917635b5c` through the tested code head changes
fourteen files: twelve CLI implementation/test files plus
`CUSTOMIZATIONS.md` and `upstream-sync-preservation.md`.

No files under Core Session execution, OpenAI Responses, compaction, provider
resolution, TUI rendering, SDK runtime, MCP runtime, or database migration were
changed by this sync. The only locally owned overlap was the CLI build/runtime
and standalone/service lifecycle boundary; it received explicit adaptation and
verification.

## Verification Evidence

- Targeted CLI tests after the final review fix: 27/27.
- Complete CLI suite on Windows: 201/201, 625 assertions. The test process PATH
  was scoped to include Git for Windows `usr/bin` for the existing `mktemp` and
  `rm` assumptions.
- CLI typecheck: passed.
- Core typecheck: passed.
- Server typecheck: passed.
- TUI typecheck: passed.
- Targeted oxlint: zero errors; warnings only.
- `git diff --check`: passed.
- Fixed build command:
  `script/build-custom-windows.ps1 -SkipInstall -RunServiceSmoke`.
- Fixed-script result: version check, Web UI check, compiled service lifecycle
  smoke, plugin discovery, authentication, contender rejection, precise stop,
  registration cleanup, and process cleanup passed.
- Final candidate:
  `D:\opencode2-zh-CN-nightly-windows-x64\opencode2-zh-CN-1.18.4-windows-x64-20260816-153906.exe`.
- Candidate size: `207018496` bytes.
- Candidate SHA-256:
  `B32A0E2BD01AE1715A88BD29FB65C6C24A3D9D41260590086DF5D43BCA14FA06`.

## Review

Review session: `ses_11111111111111111111111111`

Initial verdict: `REJECT`

The initial review found that a bare ambient `OPENCODE_CPU_PROFILE` value could
still be consumed by `ServiceConfig.options()` during a non-serve command.

Final verdict: `APPROVE WITH DEFERRED RISKS`

The same review session confirmed that `3df41c2f4` makes the original ambient
propagation counterexample unreachable and introduces no new blocking finding.

Deferred quality risks do not block this synchronization:

- No single test drives the entire explicit-parent to private-child process
  chain in one assertion. The resolver, service propagation, standalone
  lifecycle, complete CLI suite, and compiled service smoke cover the layers.
- The preservation narrative contains an earlier 199-test snapshot; the final
  synchronization evidence is the 201/201 result recorded here.

## Preservation Outcomes

| Stable ID           | Outcome   | Evidence                                                               |
| ------------------- | --------- | ---------------------------------------------------------------------- |
| `CUST-TUI-001`      | preserved | TUI prompt layout was outside the changed path set.                    |
| `CUST-CLI-001`      | preserved | Chinese command output remained unchanged; complete CLI suite passed.  |
| `CUST-I18N-001`     | preserved | Locale catalogs and selection were untouched.                          |
| `CUST-TUI-002`      | preserved | Session picker behavior was untouched.                                 |
| `CUST-TUI-003`      | preserved | Prompt footer status behavior was untouched.                           |
| `CUST-TOOL-001`     | preserved | Tool truncation and metadata paths were untouched.                     |
| `CUST-TOOL-002`     | preserved | Subagent execution and clickable output paths were untouched.          |
| `CUST-SESSION-001`  | preserved | Session compaction semantics were untouched.                           |
| `CUST-SESSION-002`  | preserved | Event sequencing and continuation paths were untouched.                |
| `CUST-SESSION-003`  | preserved | Remote automatic-compaction fail-safe paths were untouched.            |
| `CUST-SDK-001`      | preserved | SDK runtime and compatibility paths were untouched.                    |
| `CUST-MCP-001`      | preserved | MCP log routing and auth paths were untouched.                         |
| `CUST-PROVIDER-001` | preserved | Provider activation, resolution, and package authority were untouched. |
| `CUST-OPS-001`      | preserved | Fixed Windows build and compiled service smoke passed.                 |
| `CUST-OPS-002`      | preserved | Default private standalone and managed-service lifecycle tests passed. |
| `CUST-OPS-003`      | preserved | Cross-process Session leases were outside the changed path set.        |
| `CUST-OPS-004`      | preserved | Marked managed/private propagation and ambient rejection tests passed. |

## Finalization

After this record is committed:

1. Verify the integration worktree is clean.
2. Fast-forward `native-responses-v2` to the record commit.
3. Verify `native-responses-v2` and the integration branch resolve to the same
   commit and have zero ahead/behind difference.
4. Keep both integration branches as bounded recovery refs.
